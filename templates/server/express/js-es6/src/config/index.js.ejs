import dotenv from 'dotenv';
dotenv.config();

export const config = {
port: parseInt(process.env.PORT || '51210', 10),
nodeEnv: process.env.NODE_ENV || 'development',
<% if (database==='mongodb' ) { %>
    mongoUri: process.env.MONGODB_URI || 'mongodb://localhost:27017/<%= projectName %>',
        <% } else if (database==='postgresql' ) { %>
            databaseUrl: process.env.DATABASE_URL || 'postgresql://user:password@localhost:5432/<%= projectName %>',
                <% } %>
                    <% if (auth==='jwt' ) { %>
                        jwtSecret: process.env.JWT_SECRET || 'secret',
                        <% } %>
                            <% if (auth==='session' ) { %>
                                sessionSecret: process.env.SESSION_SECRET || 'secret',
                                <% } %>
                                    };